import React from 'react';
import { parsedJSON } from '../scripts/parseData';

export default class BudgetMap extends React.Component {
  componentDidMount() {
  }

  render() {
    return (
      <div className="budgetmap">
        <p> HELLO </p>
      </div>
    );
  }
}
